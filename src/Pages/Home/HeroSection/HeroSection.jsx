import React from 'react';
import { Fade } from 'react-reveal';

const HeroSection = () => {
    return (
        <div>
            <Fade bottom>
                <h1>React Reveal</h1>
            </Fade>
        </div>
    );
};

export default HeroSection;